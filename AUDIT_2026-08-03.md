# AUDIT 2026-08-03 — полный аудит «до идеала» (s26)

Три независимых прохода: Codex adversarial (клеймы vs код), Claude power-user pass
(все тексты EN/RU, 51 finding), Claude cross-tool pass (консистентность кода,
bash↔ps1 паритет). Локальные сигналы на момент аудита: shellcheck clean,
bats 49/49, verify-releases 5/5, CI всех 6 репо green, version drift не найден,
битых md-ссылок/якорей нет.

Статус: **открыт**. Чекбоксы закрывать по мере фиксов.

---

## P0 — потеря данных / нарушение core-обещания тула (Windows-порты)

> **Статус: ЗАКРЫТ 2026-08-03 (Pack #1).** securetrash `d80e012`+`782c242`,
> seedsplit `9a315b8`, ghostdraft `3064ef6`, vaultwatch `a07df34` — все с Codex-ревью
> и тестами (Pester +19, bats +1).

- [x] **P0-1 securetrash.ps1: `vault reset` уничтожает сейф ДО валидации размера.**
  `securetrash/windows/securetrash.ps1:946-948` — `Invoke-StVaultDestroyNow` до
  `Assert-StValidSize`. Опечатка в размере = невосстановимая потеря сейфа.
  Bash это чинил отдельно (`securetrash:656-657`, AUDIT_2026-07-03 P2-2) — порт отстал.
  Туда же: перед destroy нет проверки «это наш контейнер» (bash `_vault_assert_destroyable`).

- [x] **P0-2 seedsplit.ps1: `split` печатает доли без round-trip self-check.**
  `seedsplit/windows/seedsplit.ps1:300-304`. Bash проверяет восстановимость до выдачи
  (`seedsplit:423-428`) — ломаная GF-математика на Windows обнаружится только при
  восстановлении, когда оригинал сида уже стёрт. Логика (`Get-SsRecoveredSecret`) в файле есть.

- [x] **P0-3 ghostdraft.ps1: vault ищется только на `V:\`.**
  `ghostdraft/windows/ghostdraft.ps1:25`, а `securetrash.ps1:379-386` монтирует на первую
  свободную букву (обычно `D:`). Черновик с сидом молча уезжает в `%TEMP%` на диск —
  прямое нарушение тезиса тула. Фикс: читать sidecar `<vault>.mount`, как `windows/paranoid.ps1:25`.

- [x] **P0-4 vaultwatch.ps1: `stop` после Explorer-eject окирпичивает тул.**
  `vaultwatch.ps1:528-530` — исчезнувший том → `restoreOk=$false` → state-файл не удалён,
  guard уже снят → вечный `already_watching`. Bash-исключение «тома нет → restore N/A»
  (`vaultwatch:623-626`) в порт не перенесено.

## P1 — supply chain / честность security-клеймов

- [x] **P1-1 Fail-open подписи в 5 bash-инсталляторах тулов.** _Закрыто 2026-08-03
  (Pack #2): securetrash `7effada` · vaultwatch `7f389f6` · panic `b1299a0` ·
  ghostdraft `8bb24a4` · seedsplit `d565e33`, CI green ×5. Fail-closed + `type -P`
  харденинг + SECURITY.md ×5 + install.bats ×5 (fakebin, uname=Darwin для ubuntu-CI).
  Вынос verify-блока в вендорируемый фрагмент — осознанно отложен (11-я копия не
  добавлена, существующие синхронизированы)._
  `<tool>/install.sh:66-70` × 5: без ssh-keygen блок подписи молча пропускается,
  установка продолжается hash-only. Umbrella (`install.sh:116-124`, fail-closed +
  `ALLOW_UNSIGNED_LEGACY`) и все 5 install.ps1 уже закрыты — P1-4 из прошлого аудита
  не откатили в bash-тулы. Это ровно те скрипты, что живут за `curl | bash`.
  Заодно: verify-блок написан 11 раз и дрейфует — вынести в вендорируемый фрагмент.

- [x] **P1-2 Ed25519-клейм umbrella противоречит 5 per-tool README.** _Закрыто 2026-08-03
  (Pack #3): panic `eba0016` · securetrash `2b317a7` · vaultwatch `9ce8a0b` · ghostdraft
  `a5fe43f` · seedsplit `81e31e1`. Сниппеты проверяют подпись (&&-chain, проверено на
  живом релизе), «F-4» убран, residual risk назван._
  `panic/README.md:48`, `securetrash/README.md:91` (внутренний тикет «F-4» наружу!),
  `ghostdraft/README.md:51`, `vaultwatch/README.md:49` — «for that you need a signature»,
  хотя релизы подписаны с s20 (`docs/RELEASE-STATE.md:46`). Для параноидальной аудитории
  один непокрытый бейдж обесценивает остальные. Standalone-сниппеты должны верифицировать подпись.

- [x] **P1-3 install.sh говорит только по-русски — включая провал проверки подписи.**
  _Закрыто 2026-08-03 (s27, umbrella `8868f8a`): та же схема, что в пяти тулах — EN по
  умолчанию, RU через `PT_LANG`/`ST_LANG`, 23 строки в обоих языках. Поток управления,
  stderr и коды возврата не тронуты. Тесты `test/install.bats` (11) держат инварианты:
  паритет ключей, отсутствие рекурсии, `\n` в каждой printf-ветке, stderr у
  security-ошибок._
  `install.sh:109,182,190`. EN README велит прочитать скрипт перед запуском — EN-пользователь
  не может ни прочитать, ни понять security-critical ошибку. i18n как в тулах (en/ru).

- [x] **P1-4 securetrash Windows: VeraCrypt-fallback обещан, но в коде отключён.**
  _Закрыто 2026-08-03 (s27, securetrash `8979fba`): README EN+RU говорят, что это ручной
  fallback через GUI VeraCrypt, и почему автоматизация отключена (пароль в argv). Выбран
  вариант «чинить README», не код._
  `securetrash/README.md:98-101` vs `securetrash.ps1:820-822` (останавливается с manual-warn;
  то же `:880-882`, `:915-916`). Либо чинить код, либо чинить README.

- [x] **P1-5 ghostdraft Windows: RAM disk / ImDisk заявлен, но порт не поддерживает.**
  _Закрыто 2026-08-03 (Pack #3, ghostdraft `a5fe43f`): README EN+RU приведён к коду —
  vault → owner-only on-disk temp, «RAM-диска на Windows нет» сказано прямо._
  `ghostdraft/README.md:113-115` vs `ghostdraft.ps1:145-163` (override → vault → `%TEMP%`).
  Свой же `windows/README.md:57` признаёт «RAM disk … none».

- [ ] **P1-6 Внутренние отчёты в корне репо подрывают доверие.**
  `AUDIT_2026-07-03.md` («К public production не готов», без пометки о закрытии) +
  `USER_TRIAL_REPORT_2026-07-06.md` — публичны, не слинкованы, рядом с бейджем
  signed-releases. Либо date-stamped «status: closed → RELEASE-STATE» шапки и гордая
  ссылка из README (audit trail — редкий актив доверия), либо перенос из корня.
  Этот файл — та же категория: закрыть шапкой по завершении.

- [ ] **P1-7 «same signed tools» не обеспечивается runtime-ом.**
  Launcher/GUI запускают первый бинарь из PATH без проверки (`paranoid:162,171`,
  `paranoid-tray.ps1:362`). Минимум — смягчить формулировку в README/GUIDE;
  максимум — hash-pin в launcher.

- [x] **P1-8 Недокументированные escape-hatches.** _Закрыто 2026-08-03 (Pack #3, umbrella
  `d584163`): honesty-блок в README EN+RU — ALLOW_UNSIGNED_LEGACY, PT_ALLOW_PARTIAL,
  PT_ALLOW_HASH_ONLY, working-copy fallback._ `ALLOW_UNSIGNED_LEGACY=1`,
  `PT_ALLOW_PARTIAL=1` (`install.sh:113,193`) — назвать самим в README, пока не нашли за нас.

## P2 — UX, онбординг, консистентность

- [ ] **P2-1 GUIDE walkthrough ломается на шагах 5-6.**
  `GUIDE.md:167` — `seedsplit split` виснет (stdin, Ctrl-D нигде не задокументирован,
  эхо сид-фразы в scrollback); `GUIDE.md:161` шлёт в GUI, у которого нет
  инструкции установки и который README зовёт «Phase B». Фикс: TTY-prompt в seedsplit
  (или `--file` в гайде) + замена шага GUI на `panic hotkey install`.

- [ ] **P2-2 Три поведения panic не сведены.** CLI hotkey `cmd+alt-p` → `panic now`;
  GUI `⌃⌥⇧P` double-press → `--hard`; launcher key 2 → `--hard`. Лаунчер молча даёт
  жёсткий вариант (убивает cloud-демоны, чистит Recents) — `GUIDE.md:65` об этом ни слова.
  Нужна одна таблица поведения + упоминание `--hard` в описании пункта меню.

- [ ] **P2-3 «Empty» = две разные destructive-операции.** Launcher «Empty» = `vault reset`
  (crypto-shred) vs `securetrash empty` (drop-folder, best-effort). Переименовать пункт
  меню или кросс-ссылки в `GUIDE.md:223` и `securetrash/README.md:144`.

- [x] **P2-4 securetrash игнорирует пред-заданный `ST_LOCALE` — обе платформы.**
  _Закрыто 2026-08-04 (s28, securetrash `4395d7f`): оба порта уважают унаследованный
  `ST_LOCALE` (контракт vendored common.sh), детект — только fallback._
  `securetrash:19` (нет `${ST_LOCALE:-…}`) и `securetrash.ps1:19`. `ST_LOCALE=ru paranoid`
  даёт смешанный язык. Остальные 4 тула делают правильно.

- [x] **P2-5 «zero dependencies» — исключения названы.** _Закрыто 2026-08-03 (Pack #3,
  umbrella `d584163`): skhd + openssl + pwsh 7 как платформенное требование; ImDisk-клейм
  снят вместе с P1-5. MANIFEST больше не заявляет pure-Bash/zero-deps плоско._
  skhd (`panic hotkey`), openssl (`seedsplit -p`), ImDisk (ghostdraft win). Бейдж
  `README.md:11` и `MANIFEST.md:67` — сузить формулировку или дисклеймер.

- [ ] **P2-6 EN/RU расхождения, несущие ФАКТЫ:**
  - `panic/README.ru.md:32` — потеряно предупреждение «skips verification» у curl|bash;
  - `vaultwatch/README.ru.md` — отсутствует вся секция Unmount-guard (EN `:80-87`);
  - `ИНСТРУКЦИЯ.md:215` — потерян SSD-каверз из `GUIDE.md:211`;
  - `ST_LANG` нет ни в одном гайде, при этом `ИНСТРУКЦИЯ.md:53-73` показывает русский
    дашборд, который EN-locale пользователь не увидит;
  - CHANGELOGs и `docs/RELEASE-STATE.md` русскоязычные при обещании EN-parity.

- [x] **P2-7 Env-версии umbrella не соответствуют своей же документации.**
  _Закрыто 2026-08-03 (s27, umbrella `ba050be`): названы точные имена (`PT_ST_VERSION`,
  `PT_VW_VERSION`, `PT_PANIC_VERSION`, `PT_GHOSTDRAFT_VERSION`, `PT_SEEDSPLIT_VERSION`)
  и сказано, что любое другое написание игнорируется молча; stale-примеры версий убраны._
  Шапка `install.sh:26-27` обещает `PT_PANIC_VERSION=0.1.5` (stale — актуально 0.1.8),
  `README.md:72` — `PT_PANIC_VERSION=0.1.7`; фактические имена — `PT_ST_VERSION`,
  `PT_VW_VERSION` (`install.sh:89`), так что `PT_SECURETRASH_VERSION` молча игнорируется.
  Два namespace (`PT_*` vs `PANIC_*`) нигде не объяснены.

- [x] **P2-8 securetrash.ps1 — хвост паритета.** _Закрыто 2026-08-04 (s28, securetrash
  `4443e21`): добавлены `vault status` (+ честное «состояние определить не удалось» там,
  где Storage-командлеты недоступны — режима отказа, которого у hdiutil нет) и алиасы
  `-v/--version/-h/--help`; вывод переведён на stdout, warn/err — в stderr (как в bash и
  остальных четырёх портах); размеры принимают суффиксы hdiutil и разворачиваются в МБ
  для diskpart, слишком длинное число отвергается как невалидный размер (Codex review)._ нет `vault status`, нет `help/-h/--version`
  алиасов (единственный из 5 портов), вывод через `Write-Host` (не редиректится;
  `windows/paranoid.ps1:437` ждёт stdout), единицы размера несовместимы с bash
  (`5g` валидно там, отвергается тут).

- [ ] **P2-9 GUI Phase B отстал от CLI-фиксов:** `ST_VAULT_PATH` не поддержан в обоих треях
  (P0-1 прошлого аудита донесён до CLI, не до GUI: `ParanoidBar.swift:286-289`,
  `paranoid-tray.ps1:23-24`); readiness-check проверяет 3 тула из 5 и не проверяет сам
  `paranoid` (`ParanoidBar.swift:492`, `paranoid-tray.ps1:293`).

- [ ] **P2-10 Онбординг README:** Guide отсутствует в нав-баре (`README.md:15`),
  `GUIDE.md:16` ложно утверждает «`~/.local/bin` already on your PATH» (default zsh — нет;
  RU-версия честнее), `verify-releases.sh` — главный «don't trust, verify»-актив — не
  задокументирован нигде кроме русского нелинкованного `docs/TESTING.md`,
  brew-путь описан только у securetrash при формулах на все 5.

- [ ] **P2-11 Монетизация раздвоена.** `README.md:225` «no paywalls, no upsell» vs
  `gui/README.md:100` «paid tier». Сказать один раз на витрине: CLI навсегда MIT,
  опциональный GUI может стать платным. Плюс вычистить из `gui/README.md`
  maintainer-контент ($99/yr, Apple ID, инвентарь машин) — это не user-док.

- [x] **P2-12 vaultwatch: `stop` возвращает 0 при неудачном restore.** _Закрыто 2026-08-04
  (s28, vaultwatch `2ae1d3c`, ghostdraft `a439c6e`): оба порта vaultwatch выходят ненулевым
  кодом, когда restore не удался (state-файл по-прежнему сохраняется для повторной попытки);
  `--yes` добавлен в vaultwatch и ghostdraft (bash + ps1) по контракту securetrash, причём
  после `--` аргументы остаются литеральными — эту регрессию escape'а поймал Codex._ (`vaultwatch:653-657`,
  последняя команда — `warn`); `--yes` парсит только securetrash, хотя confirm есть ещё
  у двух (обходится только env `ST_ASSUME_YES`).

## P3 — полировка (после P0-P2)

- [ ] trap-гигиена: `panic:368-376` mktemp без trap + `mv` теряет права skhdrc;
  `securetrash vault create` без отката частичного контейнера; vaultwatch вообще без trap.
- [ ] `ghostdraft:214-218` — `$vol` в regex-grep (`grep -F` нужен): путь с `.` `[` `+`
  даёт ложный детект/уход с vault.
- [ ] Vault-детект — 4 реализации, 3 разных ответа (securetrash plist / launcher `-d` /
  ghostdraft mount|grep / GUI mountedVolumeURLs). Stale-каталог = «OPEN» и «closed» одновременно.
- [ ] `_fv_state` tri-state продублирован в securetrash+paranoid, а канонический
  `lib/common.sh:88-90` остался двоичным — догнать common и перевендорить.
- [ ] seedsplit: `require_macos` без причины (утилиты POSIX; ps1-порт гейта не имеет —
  Windows можно, Linux нельзя); ps1 v0.4.1 без `-p/--passphrase` при том же номере версии.
- [ ] panic.ps1: команды `hotkey` нет (честно не заявлена, но крупнейший паритет-разрыв);
  stale-док `rundll32` vs P/Invoke (`panic/windows/README.md:59`); `hotkey status` ≠
  uninstall в usage-таблице (`panic/README.md:58`).
- [ ] securetrash/vaultwatch install.ps1 — без encoding-guard при подаче SHA256SUMS
  (ghostdraft:141-143 объясняет, зачем он нужен); ghostdraft — единственный, позволяющий
  подменить pubkey через env (не помечено test-only).
- [ ] Тул-инсталляторы: `/usr/local/bin`+sudo vs umbrella `~/.local/bin` (двойная установка),
  curl-падение без пояснения, нет PATH-подсказки.
- [ ] `docs/TESTING.md:29` — версия stale третий раз подряд (0.4.11 vs 0.4.13): убрать
  номер из текста вовсе.
- [ ] Тексты: signing-параграф `README.md:139-146` — стена, RU-версия чинит это строкой
  «Коротко» (`README.ru.md:139`), у EN её нет; «movement» в `MANIFEST.md:5`; maturity-статусы
  vaultwatch/ghostdraft («early WIP») не видны в сводной таблице `README.md:41-45`;
  нет строчек LOC («small enough to read» → «~1400 строк, час чтения»);
  нет `SECURITY.md`/`CONTRIBUTING.md` в umbrella (README `:232` обещает их «в каждом репо»).
- [ ] Linux: нигде не сказано «не поддерживается и почему» — для FOSS-аудитории молчание
  читается как недосмотр (`install.sh:31` просто отшивает по-русски).
- [ ] Из Codex-ревью Pack #1 (валидные, отложены осознанно): quoting `"V:\"` в аргументах
  guard-задачи Task Scheduler (`vaultwatch.ps1:282` — backslash-перед-кавычкой argv-hazard);
  folder-mountpoint детект (`Test-VwMounted` = Test-Path, том в каталоге не ловится);
  NotContentIndexed может пережить remount тома → «restore N/A» при eject теряет
  pending restore (паритет с bash-политикой; проверить на реальном VHD — железный RUNBOOK);
  UTF-8 без BOM в ps1 при заявленном PS 5.1 baseline (mojibake RU-строк в powershell.exe).
- [ ] Мелочь: `ru:unknown_cmd` не переведён во всех 5 тулах; `attach_opts` без `local`
  (`securetrash:619`); ~~`$home` затеняет automatic variable (`paranoid-tray.ps1:14`)~~ —
  закрыто 2026-08-04 вместе с P2-9: присваивание в read-only `$HOME` бросало исключение и
  роняло определение тома целиком, а не просто «затеняло»;
  ghostdraft `Invoke-GdShred` затирает пользовательский `ST_ASSUME_YES` (`ghostdraft.ps1:200-206`);
  cross-repo ссылки «See paranoid-tools/README.md» мертвы в standalone-виде у 3 тулов.

---

## Рекомендованные packs (по одному за итерацию)

1. **Pack #1 — P0-квартет** (4 Windows-порта, каждая правка зеркалит готовый bash-фикс).
2. **Pack #2 — P1-1 fail-closed** в 5 bash-инсталляторов + вынос verify-блока.
3. **Pack #3 — Ed25519 doc-pass** (P1-2, P1-8, P2-5) — снять противоречие клеймов.
4. **Pack #4 — GUIDE walkthrough** (P2-1, P2-2, P2-3) — рабочий туториал конец-в-конец.
5. **Pack #5 — i18n install.sh** (P1-3) + EN/RU факт-парность (P2-6).
6. Дальше — P2 хвосты и P3 по списку.

Гейты Mr.Di: P1-6 (судьба публичных отчётов), P2-11 (формулировка монетизации),
P1-4 (чинить VeraCrypt-код или README).
